var.oDiv=doucment.getEelmentByName（"div"）得到的是数组 那就是oDiv是个数组，那么数组oDiv.length就会得到数组的长度
---------------------------------------------------------------------------

真的：true、非零数字、非空字符串、非空对象
假的：false,数字0、空字符串、空对象（NULL）、undefined(包括未定义和定义但是没给东西)

--------------------------------------------------------------------------
Json:
	json={a:12，b:23，c:56}  json.a=12 但是他不像数组一样可以.length
----------------------------------------------------------------------------

循环： 数组的话： for 0-length   
	json的话：for (var i in json) 就是解决json循环的存在，因为json不能求得它的长度 i=json[i]输出的结果就是 比如a=12
-----------------------------------------------------------------------------
arguments是个数组   就是我的函数没有参数变量 但是我可以传多个参数，在函数内部通过arguments.length就可以得到我输入参数的个数。有个个数那么在函数内部就可以实现功能，尤其是for


----------------------------------------------------------------------------
复合样式：background   border
单一样式:width height
